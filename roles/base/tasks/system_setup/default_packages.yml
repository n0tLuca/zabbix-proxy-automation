# Install VIM and Sudo

- name: Install VIM and Sudo
  tags: packages
  become: true
  apt:
    name:
      - vim
      - sudo
    state: present

- name: Add user to sudo group
  tags: packages
  become: true
  user:
    name: "{{ operator_user }}"
    groups: sudo
    append: yes
    state: present
