# Install VIM and Sudo

- name: Install VIM and Sudo
  tags: packages
  become: false
  apt:
    name:
      - vim
      - sudo
    state: present

- name: Add user to sudo group
  tags: packages
  become: false
  user:
    name: "{{ operator_user }}"
    groups: sudo
    append: yes
    state: present
